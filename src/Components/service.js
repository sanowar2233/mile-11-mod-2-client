import React from 'react';
import { Link } from 'react-router-dom';

const service = () => {
    return (
        <div>
            <Link to='/'>home</Link>
        </div>
    );
};

export default service;